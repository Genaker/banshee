<!DOCTYPE html>
<html lang="en" prefix="og: http://ogp.me/ns#"><head>

    <title>{% block title %}{% endblock %}</title>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1" />
    {% block meta %}{% endblock %}

    <link rel="stylesheet" href="{{ path }}themes/ytspace/upload_video.css">
    <link rel="shortcut icon" type="image/x-icon" href="{{ path }}themes/ytspace/images/favicon.png">
    <link rel="stylesheet" type="text/css" href="{{ path }}static/css/style.css%3Fv=3.7.css">
    <link rel="stylesheet" href="{{ path }}static/css/jquery-ui.css%3Fv=1.3.css" />
    <link rel="stylesheet" href="{{ path }}static/css/bootstrap.css%3Fv=1.3.css" type="text/css" />
    <link rel="stylesheet" href="{{ path }}static/css/animate.css" type="text/css" />
    <link rel="stylesheet" href="{{ path }}static/css/font-awesome.min.css" type="text/css" />
    <link rel="stylesheet" href="{{ path }}static/css/simple-line-icons.css" type="text/css" />
    <link rel="stylesheet" href="{{ path }}static/css/font.css" type="text/css" />
    <link rel="stylesheet" href="{{ path }}static/css/app.css" type="text/css" />
    <link href="http://maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css" rel="stylesheet">
    {% block head_css %}{% endblock %}


    <script src="{{ path }}static/js/jquery.min.js"></script>
    <script src="{{ path }}static/js/bootstrap.js"></script>
    <script src="{{ path }}static/js/s.js"></script>
    <script src="{{ path }}static/application.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jqueryui/1/jquery-ui.min.js" type="text/javascript"></script>
    <script type="text/javascript" src="http://cdnjs.cloudflare.com/ajax/libs/pace/0.4.17/pace.js?v=0.4.17"></script>
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
    <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->
    {% block head_js %}{% endblock %}

</head>

<body>
<input type="checkbox" id="check" style="display:none;">
<label for="check">
</label>
<label for="check">
    <div id="cover">
        <div id="box">
            <img src="http://www2.psd100.com/ppp/2013/11/2801/Close-button-1128054041.png" id="closeButton">
            <a target="_blank" onclick="chrome.webstore.install()" >
                <img src="img/chrome.jpg" width="600" onclick="imghide()">
            </a>
        </div>
    </div>
</label>

<script>
    $.detectChromeExtension = function(extensionId, accesibleResource, callback){
        if (typeof(chrome) !== 'undefined'){
            var testUrl = 'chrome-extension://' +extensionId +'/' +accesibleResource;
            $.ajax({
                url: testUrl,
                timeout: 1000,
                type: 'HEAD',
                success: function(){
                    if (typeof(callback) == 'function')
                        callback.call(this, true);
                },
                error: function(){
                    if (typeof(callback) == 'function')
                        callback.call(this, false);
                }
            });
        } else {
            if (typeof(callback) == 'function')
                callback.call(this, false);
        }
    };
    // Example call
    // please note,
    // that IE11 now returns undefined again for window.chrome
    // and new Opera 30 outputs true for window.chrome
    // and new IE Edge outputs to true now for window.chrome
    // so use the below updated condition
    var isChromium = window.chrome,
            vendorName = window.navigator.vendor,
            isOpera = window.navigator.userAgent.indexOf("OPR") > -1,
            isIEedge = window.navigator.userAgent.indexOf("Edge") > -1;
    if(isChromium !== null && isChromium !== undefined && vendorName === "Google Inc." && isOpera == false && isIEedge == false) {
        $.detectChromeExtension('abmbjjogbfkodiihiadkcgaooijdniao', 'jquery.js', myCallbackFunction);
    } else {
        // not Google chrome
    }

    function myCallbackFunction(extensionExists) {
        if (extensionExists) {
            console.log('Extension present');

        } else {
            console.log('Extension not present');
            var alerted = localStorage.getItem('alerted') || '';

            if (alerted == 1 || alerted == 2 || alerted == 3 || alerted == 4 || alerted == 5 || alerted == 6 || alerted == 7 ){

            }else{
                localStorage.setItem('alerted','0');
            }

            var alertvalue = alerted % 7;

            if (alertvalue == '0') {
                var isChrome = !!window.chrome

                if (isChrome) {
                    document.getElementById("check").checked = true;
                }

            }

            alerted++;

            localStorage.setItem('alerted',alerted);

            function imghide(){
                document.getElementById("check").checked = false;
            }

            document.getElementById('closeButton').addEventListener('click', function(e) {

                e.preventDefault();

                this.parentNode.style.display = 'none';

                document.getElementById("check").checked = false;

            }, false);


        }
    }


</script>

<div class="app app-header-fixed" id="app">

    <!-- navbar -->
    {% include 'parts/navbar.twig' %}
    <!-- / navbar -->

    <!-- menu -->
    {% include "parts/menu.twig" %}
    <!-- / menu -->
    <script>window.onload = function() {
            setTimeout(function() {
                var ad = document.querySelector("ins.adsbygoogle");
                if (ad && ad.innerHTML.replace(/\s/g, "").length == 0) {
                    ad.style.cssText = 'display:block !important';
                    ad.innerHTML = '<iframe src="themes/ytspace/images/image.html"  scrolling="no" allowtransparency="true" style="background-color: transparent;" frameborder="0" width="100%" height="90"></iframe>';
                }
            }, 2000);
        }
    </script>
    <!-- content -->
    <div class="app-content">
        <div ui-butterbar></div>
        <a "http://vimosound.com/href" class="off-screen-toggle hide" data-toggle="class:off-screen" data-target=".app-aside" ></a>
        <div class="app-content-body fade-in-up">
            <!-- COPY the content from "tpl/" -->
            <div class="hbox hbox-auto-xs hbox-auto-sm">

                {% include "parts/search-form.twig" %}


                </br>
                <section>
                    <div class="container">
                        <div class="row">

                            <script>var videos =[];</script>
                            <div class="col-md-10 content">
                                {% block panels %}

                                {% endblock %}
                            </div>
                            <!-- /.col-md-10 content -->
                        </div>
                    </div>
                </section><div style="visibility:hidden;">
                    <script>
                        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
                                    (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
                                m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
                        })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

                        ga('create', 'UA-57128250-1', 'auto');
                        ga('send', 'pageview');



                    </script>

                </div>

            </div>
            <!-- PASTE above -->
        </div>
    </div>
    <!-- /content -->




    <!-- footer -->
    {% block footer %}{% endblock %}
    <!-- / footer -->
</div>
<!-- jQuery -->
{% block footer_js %}{% endblock %}
</body>
</html>